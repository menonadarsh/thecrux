<%- include("partials/header", { title: "not found", crumb: "~/404" }) %>
<div class="blank">
  <pre class="blank-art">  ┌───────────┐
  │  4 0 4    │
  │  no such  │
  │  path     │
  └───────────┘</pre>
  <p class="muted"><code><%= name %></code> does not exist.</p>
  <a class="kbtn" href="/">cd ~</a>
</div>
<%- include("partials/footer") %>
